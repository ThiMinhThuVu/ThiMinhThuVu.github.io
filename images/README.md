# Profile image

The current portrait is `avatar.jpg`. To use another image, add it to this directory and set:

```yaml
profile_image: "/images/your-photo.jpg"
```

in `_data/profile.yml`. If `profile_image` is empty, the site uses the neutral SVG placeholder.
